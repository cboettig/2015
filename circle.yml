general:
  branches:
    only: 
      - master

machine:
  services:
    - docker

test:
  override:
    - docker create --name cache -v /data/_cache cboettig/2015-cache && docker run --volumes-from cache -e GH_TOKEN=$GH_TOKEN --entrypoint "/data/_build/deploy.sh" cboettig/2015 

