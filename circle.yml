general:
  branches:
    only: 
      - master

machine:
  services:
    - docker

test:
  override:
    - docker create --name cache -v /data/_cache cboettig/2015-cache && docker run --volumes-from cache cboettig/2015 Rscript -e 'servr::jekyll(scripts = "_build/build.R", serve = FALSE)' 

